fastapi
uvicorn
httpx
python-multipart
chromadb
pypdf
# 解密加密 PDF（AES）需要：pypdf 解析受口令/加密保护的 PDF 时依赖它，
# 缺失会报「cryptography>=3.1 is required for AES algorithm」导致 PDF 入库失败。
cryptography>=3.1
python-docx
# MySQL 驱动：纯 Python 实现，无需编译，内网友好。
# 用于持久化文档元数据（分类/描述/上传时间/状态），替换前端 localStorage。
PyMySQL
# 认证：bcrypt 做密码哈希，PyJWT 签发/校验登录令牌。均为纯 Python，内网友好。
bcrypt
PyJWT
# 内网环境需要：让 Python 使用 Windows 系统证书库，
# 解决公司安全网关拦截 HTTPS 导致的 SSL 证书验证失败问题。
pip-system-certs
# 答案层「研判」防幻觉：用 LangChain 的 ChatOpenAI(指向 DeepSeek) + 结构化输出，
# 一次调用同时判断「资料能否回答该问题」并作答，不能答则明确拒答。
# 注意：装的是 LangChain 1.x，检索/judge 组件 import 路径与网上 0.x 教程不同
# （EnsembleRetriever/CrossEncoderReranker 等在 langchain_classic.retrievers）。
# 仅 judge_service 走 deepseek provider 时才会 import，未启用 JUDGE_ENABLED 不受影响。
langchain==1.3.15
langchain-openai==1.5.0
langchain-core==1.5.4
# 检索质量专线：langchain_chroma 包住已存在的 Chroma 集合做召回适配（阶段1 地基），
# 供 rerank / 多查询改写等增强层复用。惰性 import，未启用增强项也需安装以保证 import 不报错。
langchain-chroma==1.1.0
